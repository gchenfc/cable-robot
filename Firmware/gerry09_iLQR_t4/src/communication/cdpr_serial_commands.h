#pragma once

// These are the 
enum SerialPrefixes {
  CALIBRATION = 'c',
  CONTROLLER = 'g',
  SPRAY = 's',
  DEBUG = 'd',
  // CAN_PASSTHROUGH = ? // TODO(gerry)
  LEGACY_TRACKING_CONTROLLER = 't',
  SETPOINT = 'x',
  TRACKING = 'k'
};

enum SetpointCommands {
};

enum TrackingCommands {
};
